# TraderHarness v0.1.0 Implementation Plan

## Overview

将现有 TraderHarness 升级为可发布的 v0.1.0，包含完整功能、质量文档和用户体验。

**目标**：一个开箱即用的 LLM 交易 Agent 环境，支持多 Agent 对比评估，10K star 品质。

**测试时段**：2026-03-01 ~ 2026-05-14（最近两个月，所有数据已覆盖）

---

## Phase 0: 项目 Rename（前置）

**工作量**：30 分钟

| 任务 | 文件 |
|------|------|
| 目录重命名 | `traderharness/` → `traderharness/` |
| 包名更新 | pyproject.toml: `name = "traderharness"` |
| 所有 import 替换 | `from traderharness.` → `from traderharness.` |
| CLAUDE.md 更新 | 全文替换 TraderHarness → TraderHarness |
| 测试 import 修复 | tests/ 下所有 import |
| 确认 168 tests pass | `pytest tests/ -q` |

---

## Phase 1: Tool 修复 & 增强

**依赖**：Phase 0 完成  
**工作量**：1-2 天

### 1.1 Bug 修复

| 文件 | 修改 |
|------|------|
| `tools/scripting.py:41` | `<=` → `<`（当天数据泄露） |
| `core/engine.py:255-258` | 涨停只拒绝买入、跌停只拒绝卖出 |

### 1.2 get_stock_info 接入行业数据

```python
# 当前：返回 {"stock_code": code, "name": code, "market": "sh"}
# 修改为：从 stock_registry.json 返回完整信息
async def handle_get_stock_info(params, ctx):
    from traderharness.data.stock_registry_loader import get_stock_info
    info = get_stock_info(code)
    return {"stock_code": code, "name": info["name"], "industry": info["industry"], "market": info["market"]}
```

### 1.3 get_market_overview / get_sector_summary 接入行业分类

- `get_market_overview`：按 `get_stock_industry(code)` 聚合，而非硬编码 "default"
- `get_sector_summary`：接收 `sector` 参数，只返回该行业内的股票

### 1.4 screen_stocks 增强

新增筛选条件：
- `change_pct_min/max`（涨跌幅范围）
- `volume_min`（成交量下限）
- `industry`（行业过滤）
- 排序方式：`sort_by`（change_5d / volume / change_1d）

### 1.5 错误信息充分化

所有 tool 的错误返回需区分：
- 代码不存在 → `"错误：{code} 不在股票列表中，请检查代码是否正确"`
- 日期前无数据 → `"错误：{code} 在 {date} 之前无交易数据（可能尚未上市）"`
- 停牌 → `"错误：{code} 当日停牌，无法交易"`
- 空结果 → `{"stocks": [], "hint": "无股票满足筛选条件，建议放宽条件"}`

### 1.6 测试验证

- 修改后跑 `pytest tests/ -q` 全绿
- 新增针对修复项的单元测试

---

## Phase 2: 新增 Tools

**依赖**：Phase 1 完成  
**工作量**：1 天

### 2.1 get_fundamentals

```python
# tools/fundamentals.py
async def handle_get_fundamentals(params, ctx):
    """查询股票基本面（ROE/净利润/营收/EPS），按 pub_date 历史对齐。"""
    code = params["stock_code"]
    # 从 fundamentals.parquet 加载，过滤 pub_date <= current_date
    # 返回最新可见的一条记录
```

### 2.2 get_announcements

```python
# tools/news.py
async def handle_get_announcements(params, ctx):
    """查询某只股票的近期公告列表。"""
    code = params["stock_code"]
    days = params.get("days", 30)
    # 从 announcements.parquet 过滤：
    #   stock_code == code AND announcement_time < current_date 09:30
    # 返回标题列表（最多 20 条）
```

### 2.3 get_news

```python
# tools/news.py
async def handle_get_news(params, ctx):
    """查询近期快讯/政策新闻。"""
    days = params.get("days", 7)
    keyword = params.get("keyword", "")
    # 从 news_cls.parquet 过滤：
    #   ctime 在 [current_date - days, current_date] 范围
    #   如有 keyword 则模糊匹配 content
    # 返回列表（最多 30 条）
```

### 2.4 注册到 ToolRegistry

- 在 `agents/tool_agent.py` 注册新 tools
- 更新 system prompt 里的工具列表说明

---

## Phase 3: 消息面推送

**依赖**：Phase 2 完成（数据加载逻辑复用）  
**工作量**：1 天

### 3.1 数据加载

在 `BacktestEngine` 启动时加载公告和快讯到内存：
```python
self._announcements = pd.read_parquet(announcements_path)
self._news = pd.read_parquet(news_cls_path)
```

通过 `ToolContext` 传递给 tool handlers 和晨报生成器。

### 3.2 P0 推送（持仓 + 自选股公告）

在 `AgentLoop._build_morning_brief()` 中新增：
```python
# 时间窗口：上一交易日 15:00 ~ 今日 09:30
holdings = list(ctx.portfolio.positions.keys())
watchlist = list(ctx.tool_call_cache.get("watchlist", {}).keys())
target_codes = set(holdings + watchlist)

filtered = announcements[
    (announcements["stock_code"].isin(target_codes)) &
    (announcements["announcement_time"] >= prev_close_time) &
    (announcements["announcement_time"] < today_open_time)
]
# 生成摘要文本
```

### 3.3 P1 推送（国家级政策）

```python
POLICY_KEYWORDS = ["央行", "证监会", "国务院", "财政部", "银保监", "发改委"]

policy_news = news_cls[
    (news_cls["ctime"] >= prev_close_time) &
    (news_cls["ctime"] < today_open_time) &
    (news_cls["content"].str.contains("|".join(POLICY_KEYWORDS)))
]
```

### 3.4 盘中窗口推送

开盘窗口（Phase 2）：推送 09:30~10:00 的 P0/P1 消息  
尾盘窗口（Phase 3）：推送 10:00~14:30 的 P0/P1 消息

在 `AgentLoop.run_day()` 的对应阶段注入。

---

## Phase 4: Corporate Actions

**依赖**：Phase 1 完成（引擎层修改）  
**工作量**：1 天

### 4.1 分红/送股处理

在 `BacktestEngine` 日循环中，每天开始前检查：
```python
# 加载 dividends.parquet
# 对每个持仓股票，检查 ex_date == current_date
# 如果命中：
#   - 送股/转增：position.quantity *= (1 + bonus_ratio + transfer_ratio)
#   - 现金分红：portfolio.cash += dividend_per_share * quantity
#   - 调整 avg_cost
#   - 记录到晨报："您持有的 XXX 今日除权，10送5派2元"
```

### 4.2 停牌处理

```python
# 在 TradingBus.place_order() 中：
# 如果当天该股票无行情数据（K线缺失）→ 视为停牌
price = self.get_execution_price(stock_code, window)
if price is None:
    return {"success": False, "error": f"{stock_code} 当日停牌，无法交易"}
# 现有代码已有这个判断，只需在晨报中提示停牌信息
```

### 4.3 晨报停牌/除权提示

在晨报中新增段落：
```
=== 持仓提醒 ===
  600519 贵州茅台: 今日除权，10派27.99元，到账现金 2,799 元
  000001 平安银行: 今日停牌（重大事项）
```

---

## Phase 5: Python 沙箱重构

**依赖**：Phase 2 完成  
**工作量**：2 天

### 5.1 traderharness_api 模块

新建 `traderharness/agents/sandbox/api.py`：

```python
class MarketAPI:
    """沙箱内数据网关，强制历史遮罩。"""
    
    def __init__(self, ctx: ToolContext):
        self._ctx = ctx
    
    def get_kline(self, code: str, days: int = 60) -> pd.DataFrame:
        df = self._ctx.preloaded_daily.get(code)
        if df is None:
            return pd.DataFrame()
        return df[df["date"] < self._ctx.current_date].tail(days)
    
    def get_all_daily(self, days: int = 20) -> pd.DataFrame:
        """全市场最近N天日线，一个大 DataFrame。"""
        frames = []
        for code, df in self._ctx.preloaded_daily.items():
            filtered = df[df["date"] < self._ctx.current_date].tail(days)
            if not filtered.empty:
                frames.append(filtered.assign(stock_code=code))
        return pd.concat(frames, ignore_index=True) if frames else pd.DataFrame()
    
    def get_stock_list(self) -> list[str]:
        return list(self._ctx.preloaded_daily.keys())
    
    def get_fundamentals(self, code: str) -> dict | None:
        # 从 fundamentals 数据中取 pub_date <= current_date 的最新一条
        ...

class PortfolioAPI:
    def __init__(self, ctx: ToolContext):
        self._ctx = ctx
    
    def get_positions(self) -> list[dict]: ...
    def get_cash(self) -> float: ...

class NewsAPI:
    def __init__(self, ctx: ToolContext):
        self._ctx = ctx
    
    def get_announcements(self, code: str, days: int = 30) -> list[dict]: ...
    def get_policy_news(self, days: int = 7) -> list[dict]: ...
```

### 5.2 execute_code tool

替换 run_script + read_file/write_file/list_files：

```python
async def handle_execute_code(params: dict, ctx: ToolContext) -> dict:
    code = params["code"]
    
    # 构建 API 实例
    api_module = build_api_module(ctx)  # 包含 market, portfolio, news
    
    # 在沙箱中执行，注入 traderharness_api
    sandbox = PythonSandbox(
        workspace_root=workspace.root,
        injected_modules={"traderharness_api": api_module},
        timeout=60,
    )
    result = sandbox.execute(code)
    return {"stdout": result.stdout, "result": result.result, "error": result.error}
```

### 5.3 性能优化

- `get_all_daily()` 使用预计算的全市场 DataFrame（启动时合并一次）
- 避免每次调用都 concat 5000 个 DataFrame
- 沙箱文件访问限制在工作目录

### 5.4 删除旧 tools

- 删除 `tools/filesystem.py`（read_file, write_file, list_files）
- 删除 `tools/scripting.py`（run_script）
- 从 ToolAgent 注册列表中移除
- 更新测试

---

## Phase 6: 上下文 & 缓存优化

**依赖**：Phase 3 完成（消息结构确定后优化）  
**工作量**：1 天

### 6.1 扩充静态 System Prompt

把以下内容合并到 system prompt 中（每天不变，利于缓存）：
- 详细的角色设定和交易规则
- 工具使用说明（每个 tool 的用法和注意事项）
- 行业列表（77 个一级行业名称）
- 环境规则（T+1、涨跌停、手续费说明）

### 6.2 追加式记忆

修改 `DailyMemory`：
```python
# 当前：滚动窗口（最近 5 天）
# 改为：追加模式，只在超限时压缩早期
def to_prompt_text(self, before_date, max_tokens=8000):
    entries = [e for e in self._entries if e.date < before_date]
    text = "\n".join(format(e) for e in entries)
    if estimate_tokens(text) > max_tokens:
        # 保留最近 5 天完整，更早的压缩为摘要
        ...
    return text
```

### 6.3 缓存断点标记

在 `LLMClient.chat()` 中，对支持 prompt caching 的 API 标记：
```python
# Anthropic: 在 system messages 最后一条加 cache_control
# OpenAI: 前缀自动匹配，无需显式标记
```

---

## Phase 7: 评估体系完善

**依赖**：Phase 4 完成  
**工作量**：1 天

### 7.1 沪深300 基准数据

```python
# 拉取沪深300指数日线（BaoStock 或 AKShare）
# 存储到 dataset/index_300.parquet
# 在 PerformanceMetrics 中新增 vs_benchmark 字段
```

### 7.2 Agent 行为分析指标

从 trajectory 数据计算：
```python
@dataclass
class BehaviorMetrics:
    avg_tool_calls_per_day: float      # 平均每天调用工具次数
    strategy_change_count: int          # 策略变更次数（从 finish_day summary 分析）
    stop_loss_execution_rate: float     # 止损执行率
    info_utilization_rate: float        # 推送公告后是否有反应
    avg_holding_days: float             # 平均持仓天数
    max_single_position_pct: float      # 最大单只持仓占比
    empty_days_pct: float               # 空仓天数占比
```

### 7.3 多 Agent 对比输出

```python
class ComparisonReport:
    def generate(self, results: list[EngineResult]) -> pd.DataFrame:
        """横向对比表：每个 Agent 一行，指标为列。"""
        ...
    
    def rank(self, metric: str = "sharpe_ratio") -> list:
        """按指标排名。"""
        ...
```

---

## Phase 8: Web UI

**依赖**：Phase 7 完成  
**工作量**：2 天

### 8.1 页面结构

```
app.py (main)
├── pages/
│   ├── config.py        # 回测配置（时间段、初始资金、Agent选择）
│   ├── run.py           # 运行回测（进度条、实时日志）
│   ├── results.py       # 单Agent结果（权益曲线、交易记录、指标）
│   ├── comparison.py    # 多Agent对比（并列曲线、排名表）
│   └── trajectory.py    # 决策回放（逐天查看 Agent 的思考过程）
```

### 8.2 核心功能

- 配置页：选择 Agent persona、回测时段、初始资金
- 运行页：异步执行，实时显示当前日期和收益
- 结果页：plotly 权益曲线、交易明细表、Performance 指标卡
- 对比页：多条权益曲线叠加、指标横向对比表
- 回放页：选择某天，展示晨报内容 + Agent tool calls + 决策逻辑

---

## Phase 9: README & 打包

**依赖**：Phase 8 完成（需要截图）  
**工作量**：1-2 天

### 9.1 README.md

结构：
```
# TraderHarness 🏇

一句话 tagline

## 为什么？（和现有工具的区别）
## 快速开始（3 步跑起来）
## 功能展示（截图/GIF）
## 架构图（简洁的 mermaid）
## 安装
## 使用方式
  - 命令行
  - Python API
  - Web UI
## 自定义 Agent（如何写 persona）
## 数据
## Contributing
## License
```

### 9.2 PyPI 打包

- `pyproject.toml` 配置完善
- `traderharness` 包名注册
- `pip install traderharness` 可用
- extras: `traderharness[llm]`, `traderharness[ui]`, `traderharness[all]`

### 9.3 CI（GitHub Actions）

```yaml
# .github/workflows/ci.yml
- lint (ruff)
- type check (mypy)
- test (pytest, Python 3.12)
- build (pyproject)
```

---

## Phase 10: 端到端验证

**依赖**：所有 Phase 完成  
**工作量**：1 天

### 10.1 完整回测测试

```bash
# 单 Agent 回测（2026-03-01 ~ 2026-05-14）
traderharness run --start 2026-03-01 --end 2026-05-14 --agent trend_trader

# 多 Agent 对比
traderharness run --start 2026-03-01 --end 2026-05-14 \
  --agent trend_trader --agent value_investor --agent quant_factor
```

### 10.2 验证清单

- [ ] 晨报包含 P0 公告 + P1 政策
- [ ] 盘中窗口推送盘中政策
- [ ] 分红除权正确处理（持仓数量变化、现金到账）
- [ ] 停牌股票无法交易
- [ ] execute_code 沙箱工作正常，traderharness_api 遮罩正确
- [ ] 基本面查询返回 pub_date 对齐的数据
- [ ] 涨停不能买、跌停不能卖（方向正确）
- [ ] 多 Agent 对比报告生成
- [ ] Web UI 完整流程可用
- [ ] `pip install traderharness && traderharness --help` 正常
- [ ] CI 全绿
- [ ] README 截图真实可用

---

## 时间估算

### 第一步：功能完成 + 端到端测试

| Phase | 工作量 | 依赖 |
|-------|--------|------|
| 0. Rename | 0.5 天 | 无 |
| 1. Tool 修复 | 1.5 天 | Phase 0 |
| 2. 新增 Tools | 1 天 | Phase 1 |
| 3. 消息面推送 | 1 天 | Phase 2 |
| 4. Corporate Actions | 1 天 | Phase 1 |
| 5. 沙箱重构 | 2 天 | Phase 2 |
| 6. 上下文优化 | 1 天 | Phase 3 |
| 7. 评估完善 | 1 天 | Phase 4 |
| 8. Web UI | 2 天 | Phase 7 |
| E2E. 端到端验证 | 1-2 天 | Phase 0-8 |

**小计：~12 天**（Phase 3+4 可并行，Phase 5+6 可并行 → 实际 ~9 天）

### 第二步：发布准备（功能稳定后）

| Phase | 工作量 |
|-------|--------|
| 9. README | 1 天 |
| 10. PyPI 打包 + CI | 0.5 天 |

**端到端测试要求：所有问题必须解决，不留 known issues 进入第二步。**

---

## 风险 & 注意事项

1. **BaoStock 限流** — 5 分钟线数据还在跑，如果被限流需要等待恢复
2. **沙箱安全** — execute_code 自由度高，需确保不能逃逸出工作目录
3. **Token 成本** — 端到端测试需要 LLM API 调用（用 DeepSeek 控制成本）
4. **数据一致性** — 分红除权后的价格调整需要和 K 线数据对齐验证
5. **rename 后的回归** — 全量测试确保 import 替换无遗漏
